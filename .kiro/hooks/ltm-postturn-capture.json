{
  "version": "v1",
  "hooks": [
    {
      "name": "LTM Post-Turn Capture",
      "description": "Records agent activity to LTM memory store after each turn. Disabled: ltm/bin/ltm.py capture-turn is a stub no-op; enable only after installing the full LTM script.",
      "trigger": "Stop",
      "enabled": false,
      "action": {
        "type": "command",
        "command": "python ltm/bin/ltm.py capture-turn"
      }
    }
  ]
}