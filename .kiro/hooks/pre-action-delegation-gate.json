{
  "version": "v1",
  "hooks": [
    {
      "name": "Pre-Action Delegation Gate",
      "trigger": "PreToolUse",
      "description": "Fail-closed host adapter for delegated work: requires a complete Action Record, the I/C-01 coordinator slot, one of the four roster receivers, exact paths, ownership, authorization, and delivery fields.",
      "matcher": "invoke_sub_agent",
      "action": {
        "type": "command",
        "command": "node .kiro/hooks/pre-action-delegation-gate.mjs",
        "timeout": 60
      },
      "enabled": false
    }
  ]
}