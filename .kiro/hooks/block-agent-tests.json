{
  "version": "v1",
  "hooks": [
    {
      "name": "Block Agent Test Runs",
      "enabled": true,
      "trigger": "PreToolUse",
      "description": "Hard-blocks agent-initiated test-like shell commands, gates, coverage, and browser-test runners. The hook payload has no trusted user-invocation or verify-and-gate exception signal, so the block remains unconditional; tests are user-driven only.",
      "timeout": 30,
      "matcher": "execute_pwsh",
      "action": {
        "type": "command",
        "command": "node scripts/general/block-agent-tests.mjs"
      }
    }
  ]
}