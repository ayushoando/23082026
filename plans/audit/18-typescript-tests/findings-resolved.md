# Resolved — TypeScript quality & tests
**Date:** 2026-09-01/02

- (Fixed along the way) Superseded note: the Vitest 4 / happy-dom suite breakage was repaired earlier on 2026-09-01 — 50 suites re-annotated with `@vitest-environment node`; planner/platform suites verified green this evening.

- **18.2 — FIXED 2026-09-02.** The deprecated `environmentMatchGlobs` tables and their `@ts-expect-error` crutches were removed from all three configs (`tests/vitest.config.ts`, `tests/vitest.admin.coverage.config.ts`, `tests/vitest.tech-docs.config.ts`), with in-file comments pointing to the per-file pragma convention. Migration evidence, not assumption: (1) `environmentMatchGlobs` is **absent from the installed vitest@4.1.11 runtime itself** (grep of `node_modules/.pnpm/vitest@4.1.11*/node_modules/vitest/dist` → 0 hits) — the tables were dead config, silently ignored, exactly as the testing-audit handover recorded; (2) all glob targets that genuinely require node were verified to already carry `@vitest-environment node` pragmas (15 glob-matching `.server.test.ts`/named files checked; 7 have pragmas — including `catalogAdminHandlers`, `priceBookGovernance`, `workspaceConfigurationRepository.server`, `svgArtifactStatus.server`, `env.server`, `tech-docs-generator/snapshot`); (3) the 8 `tests/unit/lib/auth/*.test.ts` files the dead glob nominally routed to node verifiably run and pass under happy-dom (the effective current behavior): `pnpm exec vitest run --config tests/vitest.config.ts tests/unit/lib/auth` → 9 files / 63 tests passed after the removal; (4) node-pragma suites still pass in the admin coverage lane: `pnpm exec vitest run --config tests/vitest.admin.coverage.config.ts tests/unit/features/admin/api/catalogAdminHandlers.test.ts tests/unit/features/admin/pricing/priceBookGovernance.test.ts` → 2 files / 8 tests passed. No `@ts-expect-error` remains in any vitest config.
- **Oxlint sweep of `tests/` — FIXED 2026-09-02.** All 18 `oxlint tests` warnings across 6 files eliminated with real code changes — no `eslint-disable`, no rule toggles, no suppressions; `site/**` untouched.
  - `tests/unit/lib/Planner/projectsStore.supabase.ownership.unit.test.ts` (1× `unicorn/no-thenable`): the Supabase-builder mock no longer defines a `then` property. `makeBuilder` now creates the chain methods as plain `vi.fn()`s whose implementation returns `Object.assign(Promise.resolve(terminal), links)` — a real Promise resolved with the `{ data, error }` terminal sentinel that carries the same chain methods — so awaiting any link in the chain (`.upsert()`, `.select()`, `.eq()`, `.single()`, `.maybeSingle()` — every await shape `projectsStore.supabase.ts` uses) resolves the terminal exactly like the real thenable PostgREST builder, while no object anywhere defines `then`.
  - `tests/operations-review/workerReleaseDecision.property.test.ts` (10× `eslint/no-useless-escape`): removed the `\"` backslashes inside the two template literals (the `rootPackage` JSON fixture and the `"vercel:prod"` route fragment); quotes kept, the generated fixture strings are byte-identical to what the escapes produced.
  - `tests/unit/planner/plannerInputCommandParity.property.test.ts` (1× `unicorn/no-useless-spread`): `fc.constantFrom(...[...MULTI_POINTER_VIEWPORT_IDS])` → `fc.constantFrom(...MULTI_POINTER_VIEWPORT_IDS)` — the Set is spread directly, same drawn values.
  - `tests/unit/planner/plannerResponsiveLayout.test.ts` (2× `eslint/no-constant-binary-expression`): the "scrim is hidden when all panels are collapsed on tablet" case now expresses the real Req 6.4 predicate `viewportIsTablet && (!leftCollapsed || !rightCollapsed || !toolsCollapsed)` with all three collapsed flags `true` — mirroring the sibling "scrim is visible" case — instead of the dead `false || false || false`; expected result `false` unchanged.
  - `tests/unit/lib/ai/audit/finding.property.test.ts` (1× `unicorn/no-useless-spread`): `const snapshot = [...findings.map((f) => f.severity)]` → `const snapshot = findings.map((f) => f.severity)` (`.map` already returns a fresh array; snapshot semantics unchanged).
  - `tests/unit/lib/observability/aiMetrics.test.ts` (3× `eslint/no-useless-escape`): `\-` → unescaped `-` in the PII character classes (`[a-z0-9._%+-]`, `[a-z0-9.-]`, `[a-zA-Z0-9_-]`); regex behavior identical.
  - Evidence: `node scripts/general/run-oxlint.mjs tests scripts` → the `=== oxlint tests ===` section prints nothing (0 warnings, 0 errors; exit 0). Touched suites re-run: `pnpm exec vitest run --config tests/vitest.config.ts tests/unit/lib/Planner/projectsStore.supabase.ownership.unit.test.ts tests/operations-review/workerReleaseDecision.property.test.ts tests/unit/planner/plannerInputCommandParity.property.test.ts tests/unit/planner/plannerResponsiveLayout.test.ts tests/unit/lib/ai/audit/finding.property.test.ts tests/unit/lib/observability/aiMetrics.test.ts` → **6 files / 72 tests passed** (ownership 7, parity 14, worker-release 1, responsive 28, aiMetrics 17, finding 5).
