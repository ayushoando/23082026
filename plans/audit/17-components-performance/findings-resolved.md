# Resolved — Component quality & performance
**Date:** 2026-09-02

- **17.1 (boundary facet) — FIXED 2026-09-02.** `site/components/Planner/Planner.tsx` now mounts a local error boundary: new `site/components/Planner/PlannerErrorBoundary.tsx` (class boundary, `logClientError` label `planner-workspace-local` with component stack) wraps the `planner-stack` workspace inside `PlannerContext.Provider`, so a render crash in a sub-panel degrades to an in-place recovery panel (reuse of the token-styled `.planner-save-state` alert — no new CSS, style-token ratchet untouched) with Reload + "Open saved plans" (/portal) instead of falling through to route `error.tsx` and losing all chrome. Errors thrown by `Planner` itself remain the route boundary's job (documented in the component header). New test `tests/unit/components/Planner/PlannerErrorBoundary.test.tsx` (renders through when no throw; fallback + logged error when a child throws). Evidence: `pnpm exec vitest run --config tests/vitest.config.ts tests/unit/components/Planner/PlannerErrorBoundary.test.tsx tests/unit/components/Planner/Planner.test.tsx` → 12 tests passed; wider touched-lane run (`about`/`career` page tests, `career`/`products` components, whole `tests/unit/components/Planner`) → 15 files / 67 tests passed. **The file-size facet of 17.1 (3,274-line component; was 3,387 at audit time) remains open** — it is the 17.2 parallel-fork/split program, not a single edit.
- **17.6 — FIXED 2026-09-02.** The flagged mojibake at `site/components/about/AboutPageView.tsx:54` is now a real em-dash, and the same single-layer mojibake em-dash (`â€"` → `—`) was fixed in the same-class comment instances in `site/components/career/CareerPageView.tsx` (3), `site/components/service/ServicePageView.tsx` (1), and `site/features/site/data/productsPage.ts` (4) — comments only, zero behavior change. Evidence: git diff shows exactly the 9 comment lines with em-dashes restored and no encoding/BOM drift. Deliberately untouched: the mojibake-cleanup code paths (`site/features/site/catalog/ProductViewer.tsx:74-77`, `site/lib/catalog/site/specSchema.ts:73` detector regex, `site/lib/catalog/site/categories.ts:107`) — those literals are intentional; and the double-encoded mojibake in `site/focss/admin/*` CSS comments (different, 2-layer defect, recorded in findings-remaining.md).
