# Remaining — i18n
**Date:** 2026-09-02

- **15.2 (broader scope):** the rest of the Planner/Studio workspace trees still hardcode user-facing English (only `PlannerEntry.tsx` was claimed by this plan's action; extending next-intl across the remaining ~175 non-i18n components is a program-level effort, not a single plan action). The `workspace.plannerEntry` keys added 2026-09-01 are the pattern to follow. Unchanged — re-verified as out of single-plan scope.
- **15.4 (residual, refined 2026-09-02):** the deferred-workflow directory `site/i18n/pending-translations/` is confirmed EMPTY on disk (re-verified 2026-09-02) and `manifest.deferredLocales` is `[]`, but physical removal is **not** an orphan cleanup and must not go through the zero-reference deletion delegation: the directory is the live output target of `scripts/export-pending-translations.mjs:81` (registered in `docs/architecture/scripts.csv`) and is contract-pinned by `tests/unit/i18n/pending-translations.test.ts` (test 1 asserts existence; test 3 `readdirSync`s it). Removing it means retiring the export/apply deferred-locale pipeline and its test — a workflow-design decision for the owner, not a cleanup. Status quo (present-but-empty, test-green in the `deferredLocales: []` branch) is coherent and costs nothing.
