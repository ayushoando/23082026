# Resolved — i18n
**Date:** 2026-09-01

- **15.1** — `site/i18n/messages/hi.json` `solutions.deliveryMedia.src` changed from `/assets/marketing/projects/DMRC/dmrc-1.webp` to the en value `/assets/marketing/hero/pages/about-oneandonly-bright.webp`. hi `alt` kept (already translated). Verified `workspace.accessPanelDescription` and all other hi content byte-identical to HEAD (only src + appended keys changed).
- **15.2 (PlannerEntry wired; plan's "starting with" scope)** — `site/components/Planner/PlannerEntry.tsx` is now an async server component using `getTranslations("workspace")`; all five strings ("Guest workspace", "Signed-in workspace", both hints, "Sign in to save", "View saved plans") come from a new `workspace.plannerEntry` subobject added to both `en.json` and `hi.json` (7 keys each, mirrored). Consumer `site/features/Planner/page.tsx` unchanged (RSC renders async component).
- **15.3** — `aria-label` now `t("plannerEntry.accessStatusLabel")` ("Planner access status" / "प्लानर एक्सेस स्थिति").
- **15.4 (resolved-stale)** — `site/i18n/pending-translations/` verified EMPTY on disk (directory listing, 2026-09-01). The deferred backlog is already drained; `manifest.deferredLocales` is `[]` and `tests/unit/i18n/pending-translations.test.ts` requires the directory to exist and contain zero `.json` files in that state. Nothing left to empty.

**Verification (real, 2026-09-01):** `pnpm exec vitest run --config tests/vitest.config.ts "PlannerEntry" "unit/i18n" "unit/lib/i18n" "scripts/check-i18n" "scripts/sync-deferred" "unit/app/(site)/access"` → 19 files / 70 tests passed, incl. `check-i18n-key-parity` runtime output "ok (locales hi)", `parity.test.ts`, `messages.test.ts`, updated `PlannerEntry.test.tsx` (now exercises the en message file via the repo's `nextIntlServerEnMock` helper and asserts the localized `role="status"` name), and `features/Planner/plannerEntryPages.test.tsx`. JSON validity + en/hi key-set equality re-verified by script (en/hi `workspace.plannerEntry` key sets identical).
