# Resolved — Accessibility
**Date:** 2026-09-01/02

- **16.2 — RESOLVED-STALE, verified 2026-09-02.** The manual focus trap in `site/components/site/MobileNavDrawer.tsx` no longer coexists with the react-aria `Modal`. Full-file read (258 lines) shows the keyboard handler at lines 137–151 is **Escape-to-close only**, with an in-code ownership comment: "Focus containment (Tab cycling) is owned by the react-aria-components Modal that already wraps this drawer — exactly one trap mechanism per component, so the two cannot drift apart." No Tab handling / focus-cycle code remains anywhere in the component; the drawer renders inside `ModalOverlay > Modal > Dialog` (lines 234–246). Exactly one trap mechanism — the finding's desired end state, landed in a prior session without the finding being updated.
- **16.4 — RESOLVED-STALE, verified 2026-09-02.** `site/components/Studio/StudioToast.tsx` now carries the same a11y upgrades as `site/components/Planner/PlannerToast.tsx`: side-by-side read shows both are 76/77-line twins whose a11y surface is byte-identical — `aria-live={kind === "error" ? "assertive" : "polite"}` on the viewport, `role={kind === "error" ? "alert" : "status"}`, and `aria-label="Dismiss notification"` on the dismiss button. The only differences are module sources (`@studio/store/studioUiStore` vs `@planner/store/plannerUiStore`, `StudioPhIcon` vs `PlannerPhIcon`) and the focss chrome path named in the EXIT_MS comment. The report-03 toast a11y backport referenced by this finding has landed in both workspaces.
