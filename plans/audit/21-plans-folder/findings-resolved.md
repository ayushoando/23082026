# Resolved — 21-plans-folder
**Date:** 2026-09-01 (remediation session; plans tree was reorganized this session — folders now under `plans/audit/`)

- **README factual errors (plan #2) — fixed by rewrite.** `plans/README.md` (restored at root during the reorg, but still describing the old layout with dead links) rewritten to describe the current tree: `plans/audit/` (36 numbered reports + 5 legacy folders + INDEX/PLAN/FIX-LOG/execution-checklist/comprehensive-code-review-report), `plans/remaining-areas/`, `plans/.archive/`. Dead links removed: `./seosec/`, `./ui-audit/`, `./client-showcase-tabs/`, `./site-ui-content-links-audit/`, `./packages/`, `./ai-audit/`, `./admin-audit/`, `./studio-audit/`, `./planner-audit/`, `./planner-comprehensive-audit/`, `./db-audit/`, `./testing-audit/`, `./worker-audit/`, `./CONTEXT.md`, `./adr/` (none exist; CONTEXT/adr noted as intentionally absent). Evidence: relative-link check over README + INDEX → "all links OK".
- **"6 unindexed folders" (plan #4) — resolved-stale.** None of `client-hub/`, `client-showcase-tabs/`, `documentation-global-standards/`, `operations-deployment-backup-review/`, `planner-comprehensive-audit/`, `site-ui-content-links-audit/` exists under `plans/` anymore (reorg; `site-ui-content-links-audit` lives at `scripts/site-ui-content-links-audit/`). The missing `client-showcase-tabs/handover.md` is moot; its record lives in FIX-LOG §5 + `audit/ui-audit/` docs. Instead, `audit/INDEX.md` was extended to index reports 30–36 (previously missing).
- **FIX-LOG pending items #9–11 (plan #3, doc half) — closed.** Index rows flipped to **fixed** with evidence after live verification: #9 showcase files all present (`site/hooks/useSectorTabs.ts`, `site/components/site/clients/*`, `ClientsPageView.tsx`, 2 e2e specs, 3+ unit tests — fs existence check); #11 `wave3-partitions.ts` + `wave5-reconcile.ts` tracked in git; #12 spot-verified (db-audit README counts, testing-audit configs, seosec statuses). §9b added recording the governance baseline re-record + README rewrite.
- **ui-audit contradiction (plan #1) — resolved-stale.** `audit/ui-audit/README.md` and `handover.md` already record deletion **then** the 2026-09-02 owner-directed reversal and re-implementation (FIX-LOG §5/§8). Live code matches the reversal (showcase implemented). No further edit needed; no new owner decision required.
- **Stale residues (plan #5) — fixed/verified:**
  - `audit/worker-audit/README.md` — "No Vectorize binding" corrected: `[[vectorize]] binding = "CATALOG_VECTORS" index_name = "catalog-nav"` verified in `workers/oando-worker-proxy/wrangler.toml`; README bindings list + issue + remedy updated (index-create + deploy remain CF-TOKEN-01-blocked).
  - `audit/24-platform-database/README.md` (ex db-audit) — counts already correct (43+21=64); its two "baseline still records 8 — re-record pending" notes updated to "re-recorded to 0 on 2026-09-01".
  - `audit/testing-audit/README.md` — already updated (lists 6 vitest configs; verified against `tests/vitest*.config.ts` on disk). No edit needed.
  - `audit/13-seo/seosec/` deferred list — already current (SEC-R07 "warning+rotation shipped, consumer removal pending"; SEC-R08 "fixed in code, migration authored-not-applied"; SEC-R09 "fixed in code"). No edit needed.
- **`audit/INDEX.md` link rot — fixed:** 02 row pointed to `.archive/` (02 lives in `audit/`), 03 row pointed locally (03 is in `.archive/`), FIX-LOG link used `../`. All corrected; reports 30–36 rows added; "Remediation corrections (2026-09-01)" paragraph appended.
- **`audit/PLAN.md` — updated:** dropped the stale "per-feature plans live under `plans/<name>/`" pointer, now describes the `plans/audit/` layout.
