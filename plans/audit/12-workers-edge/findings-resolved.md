# Resolved — Workers & edge (`workers/oando-worker-proxy`)
**Date:** 2026-09-01/02

- **12.1 (Medium) — FIXED 2026-09-02.** `src/index.js` no longer carries the hardcoded origin fallback. `wrangler.toml [vars]` `VERCEL_ORIGIN` is now the single source of truth; when the var is missing the worker fails fast with a clear 500 (`x-oando-proxy: config-error`, HSTS still set) instead of silently proxying to a stale hardcoded host — a Vercel project rename can no longer break production silently. Asset paths are unaffected (they never reach the origin branch). Evidence: repo-wide grep `oando1408.vercel.app` now matches only `wrangler.toml:12` (the configured value) plus docs (`README.md`, `worker-audit/README.md`, `plans/audit/12-workers-edge/*`) and the operations-review extractor/fixture strings; worker source keeps the `env.VERCEL_ORIGIN` dependency required by `scripts/operations-review/extractors/worker.ts:89`. New test `tests/unit/workers/originConfig.test.ts` (3 tests: literal exists only in `wrangler.toml [vars]` and never in worker source; unset var → 500 config-error; configured var → upstream URL built from the env value) — `pnpm exec vitest run --config tests/vitest.config.ts tests/unit/workers` = 2 files / 8 tests passed (with `cachePolicy.test.ts`). `README.md` "Environment Variables" updated to state the single source and the no-fallback contract. Live/deploy verification remains deploy-gated (NO deploys this session; CF-TOKEN-01 from the 09-01 sweep still stands).
- **12.6 (Low) — npm-lockfile half resolved-stale 2026-09-01 session; verified 2026-09-02.** The "npm `package-lock.json` inside a pnpm repo" complaint is addressed by documentation + wiring now present in the working tree: `workers/oando-worker-proxy/README.md` explicitly states the worker is **not** a pnpm workspace member and keeps its own npm `package-lock.json` (with `npm ci` as a documented prerequisite), and root `package.json:95-97` (`worker:dev/deploy/tail`) delegate via `pnpm --dir` without installing. Remaining half (wrangler pin skew) is recorded in findings-remaining.md — realignment needs an `npm install` in the worker dir, outside this session's allowed command set.
