# Resolved — 20-scripts-governance
**Date:** 2026-09-01 (remediation session)

- **20.1 (ratchets) — fixed.** P4 verified stale: all 64 migrations carry `-- rollback`/`-- down` (43 in `site/platform/supabase/migrations`, 21 in `migrations.admin`; 0 missing). S2 re-triaged: plans reorg (folders under `plans/audit/`) leaves exactly 1 in-scope hit (`plans/audit/comprehensive-code-review-report.md`). Baseline re-recorded `P4 8→0, S2 22→1`.
  - Evidence: `node scripts/general/check-governance.mjs` → `check:governance OK — S2_stray_report=1`; `node scripts/general/check-governance.mjs --update --confirm` → `{"D2_npx":0,"D3_dead_overrides":0,"D6_nonportable_in_gate":0,"P2_csp_unsafe_inline":0,"P4_migration_no_rollback":0,"S2_stray_report":1}`.
- **20.2 (`--update` laundering) — fixed.** `scripts/general/check-governance.mjs` now (a) refuses `--update` without `--confirm` / `GOVERNANCE_BASELINE_CONFIRM=1`, printing the pending diff; (b) refuses any update that would RAISE a rule count, even with `--confirm`.
  - Evidence: `--update` alone → exit 1 "refused… Pending change: P4 8→0, S2 22→1"; probe stray file + `--update --confirm` → exit 1 "REFUSED — this would RAISE the ratchet: S2_stray_report: 1 -> 2"; plain run with probe → exit 1 "violances increased S2 1→2"; probe removed → `check:governance OK — S2_stray_report=1`. (Probe was a self-created temp file, removed after test.)
- **Plan #1 (commit wave3-partitions.ts + wave5-reconcile.ts) — resolved-stale.** Both are already tracked and the tree is clean for that dir.
  - Evidence: `git ls-files scripts/site-ui-content-links-audit/` lists `wave3-partitions.ts` and `wave5-reconcile.ts`; `git status --short -- scripts/site-ui-content-links-audit/` → empty.
- **Rec #3 (scan:secrets in `release:gate:fast`) — resolved-stale.** `release:gate:fast` already chains `check:launch`, which runs `scripts/general/scan_secrets.mjs` (same script as `scan:secrets`). No edit needed.
  - Evidence: `package.json` — `release:gate:fast` contains `pnpm run check:launch`; `scripts/run-ops.mjs` `"check:launch"` calls `runGeneral("scan_secrets.mjs")`.
- **Plan #6 (register unregistered one-offs) — fixed (registration variant; deletion separately remains user-gated).** Re-triage found 12 zero-wiring-references scripts at `scripts/` root (findings' "~18" was stale; e.g. `planner-lift-project-trees.mjs`, `delete-twin-images.mjs`, `audit-disk-image-twins.mjs`, `merge-recovery-into-majors.mjs` now have wiring/cross refs). All 12 registered as ops commands in `scripts/run-ops.mjs`: `db:images:rewrite:apply`, `db:images:rewrite:reverse`, `assets:contact-sheet`, `audit:images:corrupt`, `verify:asset-decode`, `assets:dedup:majors`, `assets:r2:mirror`, `catalog:trim`, `audit:marketing-ui`, `audit:ui-polish-pass1`, `audit:mobile-canvas-share`, `cloudflare:security-txt`. Mutating ones carry guard comments.
  - Evidence: `pnpm run ops list` → `149 ops commands` including all 12; `node scripts/scan-boundaries.mjs` → `boundary OK`; `pnpm exec vitest run --config tests/vitest.config.ts tests/operations-review` → 16 files / 141 tests passed.
- **Plan #6b — `docs/architecture/scripts.csv` `known_route` filled for all 12 rows** (12 rows updated, `ops <name>`).
- **Plan #5 partial (stale tech-docs pages) — fixed.** `tech-docs-generator/src/pages/Security.tsx` no longer claims "product data is largely disk under site/data/storage/" — now matches `product-map.md` (disk `site/platform/*/data/` dev / Supabase prod; legacy dir retired, do not write). `Overview.tsx` already said "retired" — left as-is. (Generated docs output not re-rendered; it is git-excluded build output.)
