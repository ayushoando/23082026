# Fix Log — 2026-09-01/02 Full-Repo Verification Sweep

Living document. One entry per corrected file: **what changed** and **why** (with the evidence that flagged it). Findings below come from a four-agent verification sweep of every `plans/` folder against live code, run 2026-09-01 under owner authorization ("correct every error found; zero-error bar").

Severity: P1 = security/behavior regression, P2 = plan-claim defect, P3 = doc drift.

## Index of corrections

(Entries are appended as each fix lands.)

| # | File(s) | Severity | Reason (source finding) | Status |
|---|---------|----------|-------------------------|--------|
| 1 | `site/app/api/Planner/ai-advisor/route.ts` | P1 | PLN-FIX-04 guest 2/min inner check lost in 2026-09-01 endpoint rebuild (verified vs commit `ea54b8a`; grep `advisor-guest` → 0 hits) | **fixed** |
| 2 | `site/components/Planner/Planner.tsx` | P2 | `loadLocalBackup` had zero call sites — PLN-FIX-01 "offer restore if local backup newer" never implemented | **fixed** |
| 3 | `workers/oando-worker-proxy/src/index.js` | P2 | worker-audit README claims HSTS on all responses; missing on security.txt, R2 asset hit, R2 fallback/404 | **fixed** |
| 4 | 9 files in `site/platform/supabase/migrations.admin/` | P2 | AGENTS.md §7 requires `-- rollback`; 8 migrations had none (governance P4 baseline was tolerating them) | **fixed** |
| 5 | upload routes (`Studio/furniture/upload`, Planner pipeline) + `uploadLimits.ts` | P2 | SEC-R09: no content-length pre-check; oversized bodies fully materialized before validation | **fixed** |
| 6 | `site/app/api/customer-queries/manage/route.ts` + `site/lib/security/staticAdminToken.ts` + `.env.example` | P2 | SEC-R07: static admin token used silently; no deprecation warning or rotation support | **fixed** |
| 7 | `site/app/(site)/planner/page.tsx` | P3 | SoftwareApplication JSON-LD embedded without `sanitizeJsonForScript` unlike sibling blocks | **fixed** |
| 8 | `scripts/general/audit-sitemap-health.mjs` + `package.json` | P2 | seosec code-actionable item never built (no sitemap health audit despite plan) | **fixed** |
| 8b | `site/app/api/tracking/route.ts` + `site/platform/supabase/auth-admin.ts` + `migrations.admin/20260901120000_user_history_owner_rls.sql` | P2 | SEC-R08: service-role key exercised on public traffic; authenticated tracking now persists via anon-key client under a new owner-scoped RLS policy (anonymous cookie writes intentionally stay server-mediated — no JWT proves ownership) | **fixed** |
| 9 | `plans/client-showcase-tabs/**` implementation | P1(plan) | 31 of 33 task boxes unexecuted; hook/components/tests/e2e absent | pending |
| 10 | `plans/planner-comprehensive-audit/**` | P2 | stale checkboxes; `finalReconciliation.ts` orphaned (no test executes it) and references nonexistent `.kiro/**`; smuggled `as unknown as` cast; handover count errors | pending |
| 11 | `plans/site-ui-content-links-audit/**` | P2 | Wave 3 partitions, completion-proof + handoff generators missing though handover claims done | pending |
| 12 | plan docs (db-audit, testing-audit, documentation-global-standards, execution-checklist, seosec) | P3 | count drift (63 not 64 migrations), stale baseline wording, unrun-marked-done 8.x | pending |

## Entries

(filled in per fix)
