# Fix Log — 2026-09-01/02 Full-Repo Verification Sweep

Living document. One entry per corrected file: **what changed** and **why** (with the evidence that flagged it). Findings below come from a four-agent verification sweep of every `plans/` folder against live code, run 2026-09-01 under owner authorization ("correct every error found; zero-error bar").

Severity: P1 = security/behavior regression, P2 = plan-claim defect, P3 = doc drift.

## Index of corrections

(Entries are appended as each fix lands.)

| # | File(s) | Severity | Reason (source finding) | Status |
|---|---------|----------|-------------------------|--------|
| 1 | `site/app/api/Planner/ai-advisor/route.ts` | P1 | PLN-FIX-04 guest 2/min inner check lost in 2026-09-01 endpoint rebuild (verified vs commit `ea54b8a`; grep `advisor-guest` → 0 hits) | **fixed** |
| 2 | `site/components/Planner/Planner.tsx` | P2 | `loadLocalBackup` had zero call sites — PLN-FIX-01 "offer restore if local backup newer" never implemented | **fixed** |
| 3 | `workers/oando-worker-proxy/src/index.js` | P2 | worker-audit README claims HSTS on all responses; missing on security.txt, R2 asset hit, R2 fallback/404 | **fixed** |
| 4 | 9 files in `site/platform/supabase/migrations.admin/` | P2 | AGENTS.md §7 requires `-- rollback`; 8 migrations had none (governance P4 baseline was tolerating them) | **fixed** |
| 5 | upload routes (`Studio/furniture/upload`, Planner pipeline) + `uploadLimits.ts` | P2 | SEC-R09: no content-length pre-check; oversized bodies fully materialized before validation | **fixed** |
| 6 | `site/app/api/customer-queries/manage/route.ts` + `site/lib/security/staticAdminToken.ts` + `.env.example` | P2 | SEC-R07: static admin token used silently; no deprecation warning or rotation support | **fixed** |
| 7 | `site/app/(site)/planner/page.tsx` | P3 | SoftwareApplication JSON-LD embedded without `sanitizeJsonForScript` unlike sibling blocks | **fixed** |
| 8 | `scripts/general/audit-sitemap-health.mjs` + `package.json` | P2 | seosec code-actionable item never built (no sitemap health audit despite plan) | **fixed** |
| 8b | `site/app/api/tracking/route.ts` + `site/platform/supabase/auth-admin.ts` + `migrations.admin/20260901120000_user_history_owner_rls.sql` | P2 | SEC-R08: service-role key exercised on public traffic; authenticated tracking now persists via anon-key client under a new owner-scoped RLS policy (anonymous cookie writes intentionally stay server-mediated — no JWT proves ownership) | **fixed** |
| 9 | `plans/client-showcase-tabs/**` implementation | P1(plan) | 31 of 33 task boxes unexecuted; hook/components/tests/e2e absent | pending |
| 10 | `plans/planner-comprehensive-audit/**` | P2 | stale checkboxes; `finalReconciliation.ts` orphaned (no test executes it) and referencing nonexistent removed-external-spec paths; smuggled `as unknown as` cast; handover count errors | **fixed** |
| 11 | `plans/site-ui-content-links-audit/**` | P2 | Wave 3 partitions, completion-proof + handoff generators missing though handover claims done | pending |
| 12 | plan docs (db-audit, testing-audit, documentation-global-standards, execution-checklist, seosec) | P3 | count drift (63 not 64 migrations), stale baseline wording, unrun-marked-done 8.x | pending |

## Entries

All corrections landed today in commits `ca38567`, `e0d319f`, `2576093`, `e779df5`, `e273f2d` (working tree clean at time of writing). Format per row: **file — what changed — why (finding ref) — evidence**.

### 1. Planner audit regressions (index #1–2)

- `site/app/api/Planner/ai-advisor/route.ts` — Restored the guest 2/min inner limiter inside the request pipeline. The 2026-09-01 endpoint rebuild (verified against commit `ea54b8a`; `advisor-guest` grepped to 0 hits in the rebuilt route) silently regressed PLN-FIX-04, re-exposing unlimited guest advisor calls behind only the outer bucket. Evidence: `tests/unit/app/api/Planner/ai-advisor/plannerAdvisorWiring.test.ts` (vitest site lane).
- `tests/unit/app/api/Planner/ai-advisor/plannerAdvisorWiring.test.ts` — Added +2 tests pinning the restored behavior: third guest call in-window is rejected; authenticated traffic still rides the outer bucket only. Evidence: tests green in the lane run.
- `site/components/Planner/Planner.tsx` — Wired `loadLocalBackup`, which had **zero call sites** — PLN-FIX-01 ("offer restore if local backup newer") was claimed done but never implemented. Now: project-load restores when the local backup is newer; a `"__draft__"` restore is guarded to non-new intents; `newProject` clears the draft backup; and the backup payload strips grid/sheet decorations so it matches the server upload contract. Evidence: `plannerLocalBackup.test.ts` below.
- `tests/unit/lib/Planner/plannerLocalBackup.test.ts` — New file (~292 lines) covering restore-on-load, the draft-intent guard, clear-on-new, and decoration stripping. Evidence: green in the planner vitest lane (final full-suite rerun PENDING).
- `tests/unit/lib/Planner/plannerUploadContentLength.test.ts` — New file (~319 lines) covering upload body limits through the Planner pipeline (shared evidence with §4 SEC-R09).

### 2. Worker proxy (index #3)

- `workers/oando-worker-proxy/src/index.js` — Hoisted the HSTS constant and applied it on **all five** response paths (main proxy, security.txt, R2 asset hit, R2 fallback, 404). The worker-audit README claimed HSTS on all responses; reality set it on one path only — an audit-claim-vs-reality gap. Evidence: static diff (`e0d319f`); live header verification deferred because `worker:deploy` is blocked by CF-TOKEN-01 (§9).

### 3. Governance and migrations (index #4)

- 8 files under `site/platform/supabase/migrations.admin/` — `-- rollback` sections added to each: `20260524240000_drop_catalog_duplicates.sql`, `20260524240001_drop_dead_better_auth.sql`, `20260524240002_create_missing_tables.sql`, `20260628100000_planner_plans_and_audit.sql`, `20260713000000_price_books.sql`, `20260727010000_product_studio_drafts.sql`, `20260727020000_workspace_editor_configs.sql`, `20260727030000_product_studio_templates.sql`. AGENTS.md §7 requires `-- rollback`; the db-audit sweep found all eight shipped without one, tolerated by the governance baseline. Evidence: `check:governance` exit 0 (ledger); effective P4 debt is now 8→0.
- `config/quality/governance-baseline.json` — **Deliberately unchanged**: still records `P4_migration_no_rollback: 8` while the live count is 0. Baseline re-record to 0 is scheduled in the final sweep so the ratchet bites going forward.

### 4. seosec fixes (index #5–8b)

SEC-R09 (upload body pre-check):

- `site/lib/security/uploadLimits.ts` — Added a content-length pre-check helper so oversized bodies are rejected before full materialization; SEC-R09 found oversized uploads were buffered entirely before validation. Evidence: `tests/unit/lib/security/uploadLimits.test.ts` (extended) + `plannerUploadContentLength.test.ts` (§1).
- `site/app/api/Studio/furniture/upload/route.ts` — Route now enforces the pre-check before reading the body.
- `site/lib/Planner/plannerRequestPipeline.ts` — `readRequestBody` enforces the same pre-check for Planner uploads.

SEC-R07 (static admin token):

- `site/lib/security/staticAdminToken.ts` — New module: logs a deprecation warning when the static token is used and supports a rotation window via `CUSTOMER_QUERIES_ADMIN_TOKENS`. SEC-R07 flagged the token as used silently with no deprecation or rotation path.
- `site/app/api/customer-queries/manage/route.ts` — Switched from a silent single-token compare to the shared helper.
- `.env.example` — Documented the deprecated token and the rotation list; fallback removal scheduled 2026-12-01.
- `tests/unit/lib/security/staticAdminToken.test.ts` — New coverage (warning, rotation window, disabled fallback).

SEC-R08 (service-role on public traffic):

- `site/platform/supabase/auth-admin.ts` — Added an anon-key client factory.
- `site/app/api/tracking/route.ts` — Authenticated tracking writes now persist via the anon-key client under owner-scoped RLS; the service-role key is no longer exercised on public traffic. Anonymous cookie writes intentionally stay server-mediated — no JWT exists to prove ownership there. Evidence: `tests/unit/app/api/tracking/route.test.ts` (updated).
- `site/platform/supabase/migrations.admin/20260901120000_user_history_owner_rls.sql` — New owner-scoped RLS policy migration. **Authored, not applied** — application plus both `db:apply` dry-runs are scheduled in the final sweep (§9).

JSON-LD (index #7, P3):

- `site/app/(site)/planner/page.tsx` — Wrapped the SoftwareApplication JSON-LD in `sanitizeJsonForScript`, matching every sibling structured-data block.

Sitemap health (index #8):

- `scripts/general/audit-sitemap-health.mjs` — New health auditor (seosec's code-actionable item was never built). `package.json` — registered as `audit:sitemap-health`. `tests/unit/scripts/audit-sitemap-health.test.ts` — new XML-parse test.

### 5. Client showcase tabs (index #9) — owner re-approval, reversing the 09-01 deletion

- `site/hooks/useSectorTabs.ts` — New keyboard-navigable sector-tab hook (roving focus, ARIA wiring).
- `site/components/site/clients/ClientCard.tsx`, `ClientLogoArea.tsx`, `ClientShowcase.tsx`, `ClientTabPanel.tsx`, `SectorTabButton.tsx`, `SectorTabList.tsx` (6 components) plus `ClientShowcaseSection.tsx` — New showcase tab UI. The plan audit (index #9) found 31 of 33 task boxes unexecuted with hook/components/tests/e2e absent; owner re-approval reversed the earlier deletion decision, so the full slice was implemented. Evidence: unit tests and e2e specs below.
- `site/features/site/clients/ClientsPageView.tsx` — Integrated the section into the clients page.
- `site/i18n/messages/en.json` / `hi.json` — Added `clients.showcase` copy keys for both locales.
- `site/lib/clients/clientRegistry.ts` — Extracted pure `groupPublishedRecords` (dedupe + sector grouping) for testability; evidence: `clientRegistry.test.ts`.
- `site/public/images/clients/README.md` — Policy correction: logo assets actually live under `/assets/marketing/client-logos/`; the README's placement rule was wrong.
- `site/focss/site/components/clients/clients-showcase.css` + `site/focss/site/components/index.css` — Styles authored in the focss zone and registered; `check:style-tokens` exit 0 confirms ratchet compliance.
- 5 new unit tests — `tests/unit/components/site/clients/ClientCard.test.tsx`, `ClientLogoArea.test.tsx`, `ClientTabPanel.test.tsx`, `tests/unit/lib/clients/clientRegistry.test.ts`, `tests/unit/lib/hooks/useSectorTabs.test.ts`; plus `tests/unit/lib/clients/clientsShowcaseI18n.test.ts` (en/hi key parity, `e273f2d`).
- 2 new e2e specs — `tests/e2e/clients-showcase-keyboard.spec.ts`, `tests/e2e/clients-showcase-layout.spec.ts`. Browser run is PENDING — final sweep (ledger).
- `plans/client-showcase-tabs/tasks.md` — Checkboxes flipped to reality + reconciliation header added.

### 6. planner-comprehensive-audit closure (index #10)

- `plans/planner-comprehensive-audit/finalReconciliation.ts` — Replaced dead removed-external-spec-tree paths with their live `plans/` equivalents and fixed the stale `site/lib/Planner/plannerLoadState.ts` reference to `site/components/Planner/plannerLoadState.ts`. The module was orphaned (nothing imported it) and pointed at files that no longer exist. Evidence: `tests/unit/planner/plannerFinalReconciliation.test.ts` now executes it and asserts every ledger/preserved path exists on disk.
- `auditModel.ts` + `auditValidators.ts` — Added a legitimate authored-artifact root type for `tests/unit/planner/`, replacing a smuggled `as unknown as` cast that hid the missing type support.
- `workstream5Evidence.ts` — Fixed a duplicate-`sourceRefs` defect in `TASK_5_9_5_10_REPOSITORY_EVIDENCE`. Key evidence this module had **never been run**: it threw on first execution under the new reconciliation test.
- `tasks.md` — Boxes 1, 5.11–5.14, and 6 flipped to verified state; `handover.md` — counts corrected and a closure note added.

### 7. site-ui-content-links-audit completion (index #11)

- `scripts/site-ui-content-links-audit/wave3-partitions.ts` + `wave3-records.ts` — Wave 3 partition generator and records (added `ca38567`/`2576093`; external-tree references scrubbed in `e779df5`).
- `wave5-reconcile.ts`, `wave5-handoffs.ts`, `wave5-completion-proof.ts` — Wave 5 reconciler, remediation-handoff, and completion-proof generators; the handover had claimed these done when they did not exist.
- `cli.ts`, `index.ts`, `schemas.ts` — Wave 3/5 subcommands wired into the audit CLI.
- 4 new property tests — `property-w3-partition-isolation-closure`, `property-w5c-completion-proof-reconciliation`, `property-w5h-remediation-handoff-completeness`, `property-w5r-severity-duplicate-reconciliation` (tests/ audit-program lane).
- `plans/site-ui-content-links-audit/handover.md` + `tasks.md` — Truthfulness note added; checkboxes reconciled.
- 5 type fixes from the static-gate agent (`e273f2d`) — `wave5-reconcile.ts`: `RankedSeverity = Severity | "not-applicable"` widening plus `severityRank`/`maxSeverity` overloads (bottom severity wasn't rankable); two sha256 fingerprint-join fixes in `wave5-reconcile.ts` and `wave5-handoffs.ts` (implicit comma-join → explicit `\u0000` join, stabilizing run fingerprints); `wave5-handoffs.ts`: duplicate `validation` key removed from the emitted manifest. Evidence: typecheck exit 0 (ledger).

### 8. Documentation accuracy sweep (index #12 + scrub)

- `plans/db-audit/README.md` + `handover.md` — Migration counts corrected to 43 + 21 = 64 (the "63" claim was wrong); the "no rollbacks" claim and P4 wording updated to reflect §3.
- `plans/testing-audit/README.md` — Updated for the 6 vitest configs and the 2026-09-01 lane additions.
- `plans/ui-audit/README.md` + `handover.md` + `ui-audit-remedy-plan.md` — Showcase reversal note (§5 owner re-approval) so the remedy trail tells the truth.
- `plans/seosec/remedy-plan.md` + `security-audit-report.md` — Per-item statuses updated (R07/R08/R09 fixed; R08 migration explicitly authored-not-applied).
- `plans/execution-checklist.md` — Phase 11 rewritten to this sweep's verified state (items marked done without evidence were un-marked or annotated).
- `plans/README.md` — Plan-index rows added/refreshed (including this FIX-LOG).
- `plans/documentation-global-standards/tasks.md` — Removed references to the external spec tree.
- Root `HANDOVER.md`, `.oxlintrc.json`, `.vscode/settings.json`, `site/tsconfig.json`, `tests/tsconfig.json`, `tests/vitest.shared.ts` — All references/ignores for the removed external spec tree deleted (`e779df5`).
- Scripts + test-fixture scrub — `scripts/general/audit-eslint-disable.mjs`, `audit-gate-skips.mjs`, `audit-hollow-tests.mjs`, `check-test-layout.mjs`, `generate-test-inventory.mjs` and their `tests/unit/scripts/...` fixtures (plus `vitest-dual-lane.test.ts`, `plannerFinalReconciliation.test.ts`): external-path fixtures replaced with node_modules-style fixtures, protections preserved (out-of-root paths remain excluded).
- Pruned superseded duplicates — `plans/packages/` and `plans/planner-audit/` legacy README/report files removed where the surviving `handover.md`/`remedy-plan.md` carry the record.

### 9. Active blocker and carry-over (unchanged)

- **CF-TOKEN-01** — Cloudflare API token invalid/expired. Blocks the Vectorize index push and `pnpm run worker:deploy`; therefore §2's HSTS fix is code-complete but not deploy-verified.
- SEC-R08 migration application: `db:apply -- --dry` + `db:apply:admin -- --dry`, then apply `20260901120000_user_history_owner_rls.sql` — scheduled in the final sweep.
- Governance baseline re-record (P4 8→0) — scheduled in the final sweep (§3).
- Remaining commits are left to the owner.

## Verification ledger

**Observed this session (static gates, exit 0):**

| Command | Result |
|---------|--------|
| `pnpm run check:layout` | exit 0 |
| `pnpm run scan:boundaries` | exit 0 (no Studio↔Planner imports) |
| `pnpm run verify:focss` | exit 0 |
| `pnpm run check:style-tokens` | exit 0 (207 tokens, at baseline) |
| `pnpm run lint:ui:strict` | exit 0 |
| `pnpm run check:governance` | exit 0 (P4 live debt 0; baseline still records 8) |
| `pnpm run typecheck` | exit 0 (after §7 fixes) |
| `pnpm run typecheck:tests` | exit 0 (after test-type fixes) |
| docs checkers (check:active-docs, check-root-markdown-links, etc.) | exit 0 |
| `scan_secrets` | exit 0 |

**Scoped vitest lanes observed green (not full suite):**

| Lane | Files | Tests |
|------|-------|-------|
| Planner (`tests/unit/planner`, `tests/unit/lib/Planner`, `tests/integration/planner`) | 77 | 609 |
| Showcase (`tests/unit/lib/clients`, `tests/unit/lib/hooks/useSectorTabs.test.ts`, `tests/unit/components/site/clients`) | 5 | 54 |
| Showcase i18n parity | 1 | 10 |
| Advisor rate limiter + wiring | 7 | 41 |
| Tracking route (SEC-R08) | 1 | 10 |
| Upload limits (SEC-R09) | 1 | 9 |
| Static admin token (SEC-R07) | 1 | 9 |
| Planner local backup module | 1 | 7 |
| Planner upload content-length | 1 | 5 |
| Site-ui audit program | 11 | 36 |
| Scripts (audit fixtures) | 51 | 216 |
| Tech-docs generator | 1 | 7 |
| **Total scoped** | **158** | **1023** |

**Requires authorized run (not executed this session):**

| Check | Status |
|-------|--------|
| Full two-lane test suite (`pnpm run test`) | Requires authorized run |
| `pnpm run gate` (release:gate) | Requires authorized run |
| Browser evidence (showcase e2e, /ooplanner restore flow) | Requires authorized run |
| `db:apply -- --dry` + `db:apply:admin -- --dry` | Requires authorized run |
| SEC-R08 migration apply (`20260901120000_user_history_owner_rls.sql`) | Requires authorized run |
| Governance baseline re-record (`pnpm run check:governance --update`) | Requires authorized run |
| `worker:deploy` + live HSTS check | Requires authorized run; blocked by CF-TOKEN-01 |

**Session summary:** All code fixes, plan reconciliations, documentation updates, and static gates complete. 1023 tests observed green across scoped lanes. Full gate, browser verification, and database migrations require owner-authorized runs.
