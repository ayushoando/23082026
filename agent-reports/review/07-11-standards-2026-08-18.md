# Standards review (07–11 wave)

> **Historical (2026-08-22):** References to `plans/08-*.md`, `plans/09-*.md`, etc. are archival. Active plan: [`plans/PLAN.md`](../../plans/PLAN.md).

Agent: `01a01342-2f75-7490-84d9-d340e5a5f7c7`  
Scope: commits `467489c`…`f92fea7` + uncommitted paths named in the brief. No shell `git diff` here; review is from those files + plan/evidence on disk.

### (a) Documented-standard breaches

**Hard — browser claimed without browser proof**  
`Agents/01-standard.md` Evidence (“Do not claim browser outcomes from unit tests alone”); `Testing-handbook.md` (“Unit ≠ browser”).  
- `bf1ddae` / `plans/09-mobile-marketing.md`: MOB-04 `[x]` with CSS + FOCSS unit regex only; Task 2 notes screenshot skipped.  
- `a419bd7` / `tests/e2e/chrome-fab-viewport.spec.ts`: SITE-S08 CSS landed; e2e never run (plan 11 correctly stays 🟡 — OK there).

**Hard (plan prose only) — `results/` Markdown**  
`AGENTS.md` §3 / Trap 6: no hand-written Markdown under `results/`.  
`plans/08-mobile-audit.md` still lists Create/Modify `results/mobile-audit/README.md` and Create `defects.md`. Steps used `.txt`; disk has `index.txt` / `defects.txt` — no landed `.md`, but the Files lines still violate the rule.

**Clean on checked code:** `localhost:3000` (not `127.0.0.1`); no handwritten `any`; FOCSS under `site/focss/`; MOB-04 reuses `theme(--breakpoint-sm)` (no new cut); ops evidence is `.txt`; `studio-phone-chrome.spec.ts` / `mobile-canvas-share.mjs` do not cross-import Studio↔Planner product trees.

### (b) Smell baseline (judgement only)

**Duplicated Code** — `shell-site-fabs.css`: `(width < theme(--breakpoint-md))` and `(max-width: 639.98px)` both set the same `--site-fab-bottom*` / side vars; SITE-S08 adds `!important` anchors on the second block. Same `padding-inline: max(var(--space-4), var(--site-fab-side-left))` in home / sitemap / catalog-mobile.

**Speculative Generality** — `chrome-fab-viewport.spec.ts` compare-dock case appends an empty `data-compare-dock` marker; does not exercise real dock geometry.

**Mysterious Name / weak guard** — `responsive-audit-config.test.ts` only `toMatch(/390/)` and `/844/` on script source.

No Feature Envy / Shotgun / Middle Man worth filing beyond the repeated FAB inset consumers (intentional chrome-var pattern).
