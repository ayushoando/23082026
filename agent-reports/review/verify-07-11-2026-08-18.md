# Verification — plans 07–11 — 2026-08-18

**Iron law:** no completion claims without fresh command output. This file records what was **run this session** and what still fails.

## Commands run (fresh)

| Command | Exit | Log |
|---------|------|-----|
| `playwright … marketing-mobile-scroll + chrome-fab-viewport + studio-phone-chrome` | **1** | `results/mobile-fixes/verify-07-11-e2e.txt` — **7 failed / 16 passed** |
| `playwright … planner-phone-chrome -g 390` | **0** | `results/mobile-fixes/verify-planner-phone.txt` — **1 passed** |
| `vitest --coverage --config tests/vitest.site.config.ts` | **0** (earlier) | `results/quality/coverage-branches.txt` — summary JSON only **7 files / 88 branches** (not the COV-01 3133-branch include) |
| `node scripts/ui-polish-pass1-audit.mjs` | cancelled | `results/mobile-audit/polish-pass1.txt` **length 0** |

## Per-plan issues (not closed)

### Plan 07 — Ops
- **Done means unmet.** M1 still red (**07-M1** / **GATE-01** / **COV-01** in `Failures.md`).
- M2 headers + M3 deploy-verify evidenced earlier; M4 TTFB still open — `results/ops/ttfb-m4-status.txt` (no worker deploy SHA).
- Coverage artifact does **not** prove 90% site branches (tiny summary only).

### Plan 08 — Mobile audit
- `polish-pass1.txt` is **empty** (T2 incomplete).
- `defects.txt` still has open rows; DEF-01 still open.
- T4 guard test exists in tree; not re-run in this verification message.

### Plan 09 — Marketing mobile
- **PASS (fresh):** marketing horizontal-scroll — **16/16** green in verify log.
- MOB-03 marked resolved in defects.txt from copy in `routeCopy.ts` — **no fresh 390 screenshot** in this verify pass.
- MOB-04 CSS landed earlier; **no fresh catalog 390 shot** this verify.

### Plan 10 — Apps phone
- **PASS (fresh):** planner phone 390 — EXIT=0 (`verify-planner-phone.txt`).
- **FAIL (fresh):** studio phone — `scrollWidth=700` vs `clientWidth+1=391` (`studio-phone-chrome.spec.ts`).
- Canvas-share: no `results/mobile-fixes/canvas-share.txt` on disk this verify.

### Plan 11 — Chrome
- **FAIL (fresh):** FAB on-canvas — `.site-fab-anchor:visible` **count=0** (3 tests).
- **FAIL (fresh):** tap ≥44 — offenders include home logo link 109×28, contact submit 324×27, footer legal, etc.
- **FAIL (fresh):** cookie bar vs FAB — same zero-anchor failure.
- T5 close-out audit / `responsive-run-2` **not run**.

## Failures.md still open (related)
COV-01, GATE-01, 07-M1, UI-01, UI-02, DEF-01 (and SEO-* not in 07–11 scope).

## Verdict for workflow verification
**Plans 07–11 are not complete.** Green slices: marketing scroll (09-T4), planner phone 390 (10-T1 slice). Open fails: chrome FAB selector/visibility, tap targets, studio horizontal overflow, empty polish log, ship gate/coverage, TTFB after `worker:deploy`.
