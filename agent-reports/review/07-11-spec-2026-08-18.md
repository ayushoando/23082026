# Spec review — plans 07–11

Agent: `01a01342-2f76-7180-8fd6-80f3490f4de5`  
Fixed point `8b48c8ab` → HEAD `f92fea7` + uncommitted 10/11 artifacts. Evidence checked on disk.

### (a) Missing / partial
- **07 Done means:** “Release gate recorded green; … TTFB back under ~250ms” — M1 still red (`Failures.md` **07-M1**); **M4** “TTFB fix” ⬜; live HSTS `2592000` ≠ intended `31536000`.
- **08 T2:** “Produces: `results/mobile-audit/polish-pass1.txt`” — file **empty**.
- **08 Goal / T4 Interfaces:** inventory “at 390×844 and **768×1024**” / assert “**768** widths” — script is phone+**1920** only; guard never asserts 768.
- **09 T4:** “Test: `tests/e2e/marketing-mobile-scroll.spec.ts` (new)” — absent.
- **09 MOB-03:** still 🟡 (“Confirm … in the browser”) though Task 0 `[x]`.
- **10 T1:** “Produces: … `planner-phone-chrome.txt`” green — run **failed** `UI-MOB-03` `more-actions(33.1×50.5)` (duplicate id in selector list); no fix.
- **10 T2/T5:** studio spec + `mobile-canvas-share.mjs` uncommitted, **not run**; no `canvas-share.txt`.
- **11 T1 Step 4 / T3–T5:** “Rerun” e2e not run; tap / cookie / close-out ⬜.

### (b) Scope creep
- **11 Architecture:** “Fixes stay in the **CSS-variable layer**” — `a419bd7` also adds `!important` left/right/`max-width` rules on `.site-fab-anchor-*` in `shell-site-fabs.css`.

### (c) Looks done, looks wrong
- **08 T4 Interfaces** vs test: guards `390`/`844`/`1920`, not **768**.
- **10 T2 Interfaces:** “`studio-mobile-shell` testid visible” — draft asserts `studio-workspace` and comments there is no `studio-mobile-shell`.
- **10 T5:** “canvas bounding box ÷ shell bounding box” — script uses **height/height** only (`DB1 canvas/shell height`).
- **11 T1 Interfaces:** “bounding box fully inside the viewport” — CSS landed; Playwright never green; `639.98px` vars largely mirror existing `< md` values; compare-dock case injects a marker, not a real dock.
- **10 Status / 99:** still “0/5 · T1 not run” while T1 log + T3/T4 no-op exist — tracker lag, not product proof.
